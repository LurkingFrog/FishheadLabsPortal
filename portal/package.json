{
  "name": "portal",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w -ws 0.0.0.0:9082 < /dev/zero",
    "clean": "bsb -clean-world",
    "server": "moduleserve ./ --host 0.0.0.0 --port 9080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "BuckleScript",
    "ReasonReact",
    "reason-react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "reason-react": ">=0.9.1"
  },
  "devDependencies": {
    "bs-platform": "^8.2.0",
    "moduleserve": "^0.9.1"
  }
}
